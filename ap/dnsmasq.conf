interface=wlan0
bind-interfaces

# Disable DNS — DHCP only
port=0

# DHCP pool and lease time
dhcp-range=10.133.7.100,10.133.7.200,255.255.255.0,12h

# Tell clients that the AP is their default gateway
dhcp-option=3,10.133.7.1

log-dhcp
